/*

import { annotate, annotationGroup } from "https://unpkg.com/rough-notation?module"


const logo = ["perfil","criar","contados"]
const forms = ["box", "circle","highlight"]

for (let i = 0; i<3; i++){
let marcacos = document.getElementById (logo[i])
let forma = forms[i]
let desenhar = annotate(marcacos, {
        type: forma,
        padding: 10, // [, ]
        color: "yellow"
})
desenhar.show()
}
*/
